<%- include('../includes/head') %>
<%- include('../includes/navigation') %>

<br>
<% if(products.length > 0) { %>
    <% for (let product of products) { %>
        <div class="card">
            <div class="card-header text-center">
                <h1>
                    <%= product.title %>
                </h1>
                <pre> <%= product.description %> </pre>
            </div>
            
            <div class="card-footer">
                <h2>Price : <%= product.price %></h2>
                <br>
                <span>Quantity : </span>
                <a href="">-</a>
                <input type="text" id="quantity" value="<%=product.cartItem.quantity%>">
                <a href="">+</a>
                <br>
                <br>
                <a class="btn btn-success" href="/product/deleteFromCart/<%=product.id%>" >Delete Product</a>
                <br>
            </div>
            <br>
        </div>
        <hr>
    <% } %>
    <div class="text-center">
        <form action="/postOrder" method="POST">
            <button type="submit" class="btn btn-primary">Order Now!</button>
        </form>
    </div>
<% } else { %>
    <h3 class="text-center text-primary">No Products Yet!</h3>
<% } %>

<script>
    
</script>

<%- include('../includes/end') %>

